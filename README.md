# my-rust-samples

自分用のRust学習の備忘録

## 逆引き辞書・サンプル集

### 1. 基本的な入出力

- [ユーザー入力の取得 (f64)](./src/bin/getting_numbers.rs)
  - `std::io` を使用した標準入力の受け取り方。
  - 文字列から数値（`f64`）への変換と、`input.clear()` による変数の再利用。

---

## 実行方法

`cargo run --bin ファイル名` で実行できます。
例：`cargo run --bin getting_numbers`
